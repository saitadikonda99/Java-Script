// Implement a function that reads a JSON file
// and returns the parsed data. Handle the
// exception when the file is not found or cannot be
// parsed as JSON, and provide appropriate error messages.


function read () {
    fetch('https://pokeapi.co/api/v2/pokemon/ditto')
        .then((res) => {
            if(respons)
        })
}